import { I{{pascalCase name}} } from '~/pages/{{pascalCase name}}/{{pascalCase name}}/{{pascalCase name}}.types'

export const use{{ pascalCase name }} = (props: I{{pascalCase name}}.IModelProps ): I{{pascalCase name}}.IModel => {
  return {}
}